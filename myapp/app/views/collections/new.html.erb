
<%= link_to("<<< Return back to Collection", collections_path, :class => 'back-link') %>
<div class="book new">
  <h2>Create Book</h2>

  <%= form_for(@book, url: collections_path) do |f| %>

    <%= render(:partial => 'form', :locals => {:f => f}) %>

    <div class="form-buttons">
      <%= f.submit("Create") %>
    </div>

  <% end %>
</div>
